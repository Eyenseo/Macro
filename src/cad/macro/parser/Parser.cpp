#include "cad/macro/parser/Parser.h"

#include "cad/macro/ast/Scope.h"

namespace cad {
namespace macro {
namespace parser {
// Parser::Parser() {
// }

// ast::Scope Parser::parse(std::string macro) const {
//   // auto root = ast::Scope(Token(0, 0, ""));

//   // root.append(ast::Scope(Token(0, 1, "")));

//   // return root;
// }
}
}
}
